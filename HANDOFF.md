# Handoff

Current state: empty Phase 1 scaffold. Planned container name is
`sagetv-server-26-gpu-j11`; clean appdata is
`/mnt/user/appdata/sagetv-server-26-gpu-j11`. It must run beside the existing
server using a user-selected address on Unraid `br0`.

